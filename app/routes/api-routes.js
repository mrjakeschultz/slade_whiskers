const express = require("express");
const router = express.Router();
const sequelize = require("sequelize");

const connection = require("./connection");

// Add routes for '/api' here

module.exports = router;
